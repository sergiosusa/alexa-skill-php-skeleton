{
    "version" : "1.0",
    "response": {
        "outputSpeech": {
            "type": "PlainText",
            "text": "{{ text }}"
        },
        "shouldEndSession": "{{ shouldEndSession }}"
    }
}

